# Archon Desktop Client Releases

Private release host for the Archon desktop (Electron) client's installer builds.
No source code lives here -- see the main `commander-pod` repo for that. This repo
exists solely so `electron-updater` has a place to check for and download new
versions of the packaged Windows installer.
